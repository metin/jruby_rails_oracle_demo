<h1>Listing depts</h1>

<table class="table table-bordered">
  <tr>
    <th>DID</th>
    <th>Budget</th>
    <th>Manager</th>
    <th></th>
    <th></th>
  </tr>

<% @depts.each do |dept| %>
  <tr>
    <td><%= dept.did %></td>
    <td><%= dept.budget %></td>
    <td><%= dept.managerid %></td>
    <td><%= link_to 'Edit', {:action=>:edit, :id => dept.did}, {:method=>:get} %></td>
    <td><%= link_to 'Destroy', '' %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Dept', new_dept_path %>
