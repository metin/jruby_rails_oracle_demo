<h1>Listing works</h1>

<table class="table table-bordered">
  <tr>
    <th>DID</th>
    <th>EID</th>
    <th>PCT</th>
    <th></th>
    <th></th>
  </tr>

<% @works.each do |work| %>
  <tr>
    <td><%= work.did %></td>
    <td><%= work.eid %></td>
    <td><%= work.pct_time %></td>
    <td><%= link_to 'Edit', '' %></td>
    <td><%= link_to 'Destroy', '' %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Work', new_work_path %>
